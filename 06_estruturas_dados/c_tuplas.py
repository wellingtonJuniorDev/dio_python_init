frutas = ("laranja", "pera", "uva",) # virgula no final para deixar explicito que eh uma tupla, e nao precedencia de operacoes

letras = tuple("python")

numeros = tuple([1,2,3,4])

print(numeros)

frutas[0]
frutas[-1]

matriz = (
    (1,"a", 4),
    (2,"a", 5),
    (3,"a", 6),
)

numeros.count(1)
len(numeros)
